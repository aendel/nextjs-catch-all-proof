{
  "name": "next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "export:serve": "npm run export && npx serve out"
  },
  "dependencies": {
    "next": "^12.1.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
